version: "3.7"

services:
  ruby:
    build: .
    command: bundle exec rackup --host 0.0.0.0
    environment:
      RUBYOPT: -EUTF-8
      TZ: Asia/Tokyo
    ports:
      - 127.0.0.1:9292:9292
    volumes:
      - .:/app
    working_dir: /app
