version: "3.7"

services:
  node:
    command: npm run serve
    environment:
      SHELL: /bin/sh
    image: node:13.1.0-alpine
    ports:
      - 5000:5000
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    working_dir: /app

volumes:
  node_modules:
